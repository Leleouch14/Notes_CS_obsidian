 [[Python Index]]
## random info
>list.index(x)
checks for the position of “x” in the said list!
> to check something at a said position in the list we use 
list[*position*]  
To import Smt from a file we can use:
> from file_name import wtv_you_importing

---
# dictionaries
1. while looping through a dictionary, the iterable object only obtains the KEY and not the value. 
	`for i in dic_1:
		`print(i)`
	here we should note that only the keys of dic_1 will be printed
2. in the case of VALUE in a key-value pair, we can also use LIST or DICTIONARIES as values.
	`{ key: [Listt]
	  `key2: {Dictionary}`
	this is called *nesting* and it is used to store much more complex data for a key.
3. `L = ["a", "b", ["c", "d"]]`
	`print(L[2][1])` 
	here the print will give out *d* as result. This is called nested list
4. Rewatch 69 from 100 days of python. 
5. 